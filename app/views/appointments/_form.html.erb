
<%= form_for @user.appointment.build do |f| %>



<!-- #apartment.build -->
    <%= f.label :address, "address" %>
    <%= f.text_field :address %>
    <br>
    <%= f.label :available_times, "available_times" %>
    <%= f.datetime_select :available_times, :ampm => true, :minute_step => 30 %>
    <h4>Add image?</h4>
    <br>

    <br>
    <%= f.submit %>
<% end %>
